'use client';

import { PublicNav } from '@/components/site/public-nav';

/** Landing surface of the one shared public navigation system. */
export function LandingNav() {
  return <PublicNav variant="landing" />;
}
