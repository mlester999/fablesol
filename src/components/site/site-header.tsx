'use client';

import { PublicNav } from './public-nav';

/** Inner-page surface of the one shared public navigation system. */
export function SiteHeader() {
  return <PublicNav variant="inner" />;
}
